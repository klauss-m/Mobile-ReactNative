/**
 * @format
 */

import {AppRegistry} from 'react-native';
import VendaProdutos from './src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => VendaProdutos);
